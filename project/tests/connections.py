APP_HOST = 'test-app'  # Using external link alias
APP_PORT = '8080'  # Should be same as in app config
DB_HOST = 'db-server'  # Using external link alias
DB_PORT = '3306'  # Should be same as in DB config
DB_USERNAME = 'test_qa'  # Should be same as in DB config
DB_USERPASS = 'qa_test'  # Should be same as in DB config
DB_NAME = 'technoatom'  # Should be same as in DB config
